프로젝트 목표 : 드래그 앤 드롭으로 메뉴를 주문할 수 있는 웹페이지를 생성한다

파일별 특징

index.html
    이 파일은 모든 프로그램을 모이는 장소이자 실제로 실행되는 장소이다
    각 프로그램이 이 사용이 될 수 있도록 틀을 만들어야 한다

    필요한 영역
        - 간판(header)
            페이지 최상단에 존재하며 페이지 이름과 
        - 메뉴판
            페이지 중심이자 가장 넓은 영역을 가지고 있다
            실제 메뉴가 있을 공간으로 메뉴는 이미지, 이름, 가격이 표기 되어야 한다
            페이지 내의 메뉴는 장바구니로 drag & drop이 가능하며 drop시 메뉴가 옮겨진다
        - 장바구님
            구매한 메뉴가 있을 영역이다
            이름, 가격, 갯수와 그 갯수를 추가하거나 감소(1 미만으로 내려가지 않는다)할 수 있는 영역이 되어야 한다
            페이지 내의 메뉴는 메뉴판으로 drag & drop이 가능하며 drop시 메뉴가 옮겨진다
            또 최하단에 구매 버튼이 있으면 누를 시 지금 구매한 메뉴에 대한 정보와 가격, 그 모든 것을 합한 가격등을 출력해야 한다
        - 카테고리 (선택사항)
            메뉴의 카테고리가 적혀있다
            카테고리를 클릭 시 해당 카테고리에 들어가는 메뉴만 나타난다

style.css
    이 파일은 html 태그의 모양을 꾸미는 것을 넘어 특정 태그의 상태를 부각할 때도 사용된다
    특이사항으로는 기본적이거나 상시로 적용되는 속성은 위에 써야하며 특정 조건이나 변경시 적용되는 속성은 아래에 적용해야 한다

    이미지 묘사
        상단 헤더에 원생으로 한 줄을 긋는다
        그리고 그 밑 하단에 카테고리를 넣는다

index.js
    이 파일은 각 객체의 이벤트를 적용하는 페이지이다
    
    필요한 영역
        전역 variable 영역
            코드 실행 시 필요한 전역 변수를 초기화는 영역

            필요한 변수
                -현재 드래그 중인 메뉴
                -현재 드래그 중인 객체가 올라간 menu 객체
                -현재 드래그 중인 객체가 올라간 box 객체

        function 영역
            각 함수를 정리한 영역으로 적용시킬 위치에 따라 

            필요한 변수
                -menu에 필요한 함수
                

        event 영역 
            특정 상환에서 객체에 함수를 실행 시키는 영역



프로젝트 진행순서
1, 프로젝트에 대한 설명을 최대한 상세히 쓴다
2, html, css등 기본적인 영역을 먼저 생성한다
3, 객체에 맞는 이벤트를 js에 작성한다